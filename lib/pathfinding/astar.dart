class AStarNode {
  final int x, y;
  AStarNode(this.x, this.y);
}

class AStar {
  List<AStarNode> findPath(
    AStarNode start,
    AStarNode goal,
    List<AStarNode> obstacles,
  ) {
    // TODO: incolla qui il tuo algoritmo A* aggiornato
    return [];
  }
}
